# ── AI Providers ──────────────────────────────────────────────────────────────
# Primary: Google Gemini (https://aistudio.google.com)
# Required. Cascade: Gemini 2.5 Pro → Flash 2.5 → Flash 2.0 → fallbacks
GEMINI_API_KEY=AIzaSy...

# Fallback 1: DeepSeek V3 (https://platform.deepseek.com)
# Optional paid fallback. Used when Gemini quota is exhausted.
DEEPSEEK_API_KEY=sk-...

# Fallback 2: OpenRouter (https://openrouter.ai) — free account, no card needed
# Gives access to Gemini 2.0 Flash free tier and Llama 3.3 70B free tier.
# Strongly recommended — kicks in before Groq so the app stays up when Gemini hits daily quota.
OPENROUTER_API_KEY=sk-or-v1-...

# Fallback 3: Groq (https://console.groq.com) — free account, no card needed
# Llama 3.3 70B, 14,400 req/day. Last resort in the cascade.
GROQ_API_KEY=gsk_...

# ── Database ──────────────────────────────────────────────────────────────────
# Supabase Postgres (https://supabase.com) — free tier works fine
# Used for: simulation snapshots, watchlists, price alerts, API keys
DATABASE_URL=postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
DIRECT_URL=postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres

# ── Auth ──────────────────────────────────────────────────────────────────────
# Supabase project credentials — found in Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://[ref].supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhb...
SUPABASE_SERVICE_ROLE_KEY=eyJhb...

# ── Caching & Rate Limiting ───────────────────────────────────────────────────
# Upstash Redis (https://upstash.com) — free tier works fine
# Optional but recommended. Without it the app runs with in-memory cache (no persistence).
UPSTASH_REDIS_REST_URL=https://[name]-[region].upstash.io
UPSTASH_REDIS_REST_TOKEN=AX...

# ── App ───────────────────────────────────────────────────────────────────────
# Full URL of the deployment. Used for server-to-self API calls.
# Local: http://localhost:3000  |  Production: your Vercel URL
NEXT_PUBLIC_APP_URL=http://localhost:3000
